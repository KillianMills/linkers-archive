# Example .env file for development — copy to `.env` and fill the values.
# Do NOT commit your real `.env` file (it's ignored via .gitignore).

# Discord bot token (required to run collectors)
# Get this from https://discord.com/developers/applications/<your-app>/bot
DISCORD_TOKEN=your-discord-bot-token-here

# Collector mode: 'discord' (default) or other collectors if implemented
COLLECTOR_MODE=discord

# Path to sqlite DB (relative to repo root)
DATABASE_PATH=data/links.db

# Optional: log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Notes:
# - If your bot uses the Message Content privileged intent, enable it in the
#   Discord Developer Portal (Bot -> Privileged Gateway Intents -> Message Content).
# - Keep your real .env private and never push it to version control.
